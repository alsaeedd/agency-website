# Authorised sellers for raltech.dev AND its subdomains.
#
# This file lives on the ROOT domain because that is where ad buyers crawl it;
# a subdomain file is only honoured when the root points at it, which the
# subdomain= line below does.
#
# This site itself runs NO ads. The entry exists so that Letterlock
# (letterlock.raltech.dev) can serve AdSense; AdSense only accepts root domains
# in its Sites list, so the whole domain has to be authorised.
#
# pub-7138183978612183 is the RAL SOFTWARE SERVICES publisher id, shared by
# AdMob and AdSense. f08c47fec0942fa0 is Google's certification-authority id
# and is identical for every Google publisher.
google.com, pub-7138183978612183, DIRECT, f08c47fec0942fa0
subdomain=letterlock.raltech.dev
